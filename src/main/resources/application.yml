spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-tiny-glitter-alwvzc7s-pooler.c-3.eu-central-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require}
    username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
    password: ${SPRING_DATASOURCE_PASSWORD:npg_Gri0T3HPCOQo}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  jwt:
    secret: "furew9doij2130jrj340r2ikeik23j9r04j9j1ir90ei3290e"
    accessTokenMinutes: 60
  h3:
    defaultResolution: 9
  books:
    seedOnEmpty: ${APP_BOOKS_SEED_ON_EMPTY:true}
    seedFile: ${APP_BOOKS_SEED_FILE:books.json}
    googleApiKey: ${GOOGLE_BOOKS_API_KEY:}
    maxResultsPerQuery: ${APP_BOOKS_MAX_RESULTS_PER_QUERY:1}
    requestDelayMs: ${APP_BOOKS_REQUEST_DELAY_MS:150}
    maxAttempts: ${APP_BOOKS_MAX_ATTEMPTS:3}
    retryBackoffMs: ${APP_BOOKS_RETRY_BACKOFF_MS:600}

logging:
  level:
    root: INFO
    bookcircle: INFO
